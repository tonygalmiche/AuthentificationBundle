{% extends "OVEAuthentificationBundle:Default:ove-base.html.twig" %}
{% block content %}
<h1>Ajouter une nouvelle association</h1>

<form action="{{ path('association_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Créer</button>
    </p>
</form>

<a href="{{ path('association') }}" class="round button dark menu-add image-left">Retour à la liste</a></li>

<!--
<ul class="record_actions">
    <li>
        <a href="{{ path('association') }}">
            Retour
        </a>
    </li>
</ul>
-->

{% endblock %}



{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset("bundles/oveauthentification/js/Association.js")}}" type="text/javascript"></script>
{% endblock %}



