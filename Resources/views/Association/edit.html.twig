{% extends "OVEAuthentificationBundle:Default:ove-base.html.twig" %}
{% block content %}
<h1>Modifier l'association</h1>

<form action="{{ path('association_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit">Sauvegarder</button>
    </p>
</form>

<a href="{{ path('association') }}" class="round button dark menu-list image-left">Retour à la liste</a></li>


<!--
<ul class="record_actions">
    <li>
        <form action="{{ path('association_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Supprimer</button>
        </form>
    </li>
</ul>
-->



{% endblock %}



{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset("bundles/oveauthentification/js/Association.js")}}" type="text/javascript"></script>
{% endblock %}



